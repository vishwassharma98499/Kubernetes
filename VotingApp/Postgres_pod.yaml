apiVersion: v1
kind: Pod
metadata:
  name: postgres-app-pod
  labels:
    name: postgres-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: postgres-app-pod
      image: postgres
      ports:
      - containerPort: 5432


